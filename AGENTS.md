# 项目管理台仓库协作约束

## 仓库边界

- 本仓库工作目录为 `D:\Projects\project-launcher-workbench`。
- 默认只允许在本仓库内创建、修改、移动或删除文件。
- 不要直接修改 `D:\Projects\` 下其他项目或任何其他外部目录中的源码、配置、脚本、测试、文档、依赖锁文件和 Git 状态。
- 不要对外部项目执行会改变工作区或仓库状态的命令，包括格式化、自动修复、依赖安装、`git add`、`git commit`、`git reset`、`git checkout`、`git stash`、`git push` 等。
- 可以按任务需要对外部项目进行只读检查，用于定位问题和收集证据；必须保留并尊重外部项目已有的未提交改动。

## 外部项目需要修改时的处理方式

当问题的真正修复需要修改其他目录下的项目时，不要直接实施修改。应在本仓库的 `docs/handoffs/` 下生成一份可交给对应项目 Codex 执行的完整修改文档，并在最终回复中提供文件链接。

交接文档至少必须包含：

1. 任务标题、目标项目路径、任务边界和禁止修改范围。
2. 问题现象、影响范围和可复现条件。
3. 根本原因、调用链或进程链分析，以及支持结论的文件、代码位置、日志或运行证据。
4. 与本管理台相关的接口契约、环境变量、配置字段、日志目录和兼容性要求。
5. 建议修改的文件清单，以及逐文件修改方案、关键逻辑或伪代码。
6. 正常路径、失败路径、手动启动、托管启动、多实例和停止行为等边界情况。
7. 必须新增或修改的测试、具体测试命令、预期结果和验收标准。
8. 安全风险、数据风险、进程归属风险、向后兼容风险及相应防护措施。
9. 回滚方法、联调顺序，以及完成后需要回传的修改文件、测试结果和 commit SHA。

文档应达到“目标项目 Codex 无需重新猜测需求即可独立实现和验证”的完整程度。不要只提供一句建议、零散代码片段或缺少测试方法的简略说明。

## 跨项目协作模式

- 本仓库 Codex负责管理台自身的配置契约、环境变量注入、进程管理、日志诊断、界面和测试。
- 外部项目 Codex负责其项目内部的启动器、业务进程、项目配置和项目测试。
- 两边通过 `docs/handoffs/` 中的交接文档协调，不在同一个外部工作区并发写入。
- 如果双方需要修改同一接口，先由本仓库定义稳定契约，再由外部项目实现；最终回到本仓库执行只读联调和验收。
- 未获得用户新的明确指示时，不要把交接文档中的方案直接应用到外部项目。

## 文档与编码

- 交接文档默认使用中文和 UTF-8。
- 文件路径、命令、环境变量、配置字段、API 名称和代码标识保留英文原文。
- 不在文档中记录密码、Token、API Key、Cookie、私有凭据或完整敏感环境变量。
