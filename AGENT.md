# Project Agent Notes

## Git Remote

- Use SSH for this repository's GitHub remote.
- Remote URL: `git@github.com:wojimmy666-code/project-launcher-workbench.git`
- Push with `git push origin main`.
- Do not switch `origin` back to HTTPS unless the user explicitly asks.
